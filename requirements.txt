# Regenerate every number and figure in the paper.
# Exact pins (==) read from the `supercon-repro` environment used for the v2 manuscript
# (importlib.metadata, 3.13.15 interpreter). v1 shipped >= ranges; the manuscript
# states the versions are fixed, so they are fixed here.
# python==3.13   (interpreter, 3.13.15 used; kept as a comment because the interpreter is not pip-installable)
numpy==2.3.3
pandas==2.3.2
scipy==1.16.2
scikit-learn==1.7.2
xgboost==3.0.5
matminer==0.9.2
pymatgen==2025.6.14
matplotlib==3.10.6
